# Vuk-ROM

VukROM is a custom scripting environment I made for fun.  
It parses `.rom` files, executes commands, and can chain into external executables (`.exe`).

I wrote this in Net10 C# as I love coding in C# and I praise Microsoft.

Syntax in Syntax.txt
